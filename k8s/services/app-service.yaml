apiVersion: v1
kind: Service
metadata:
  name: gobeze-ai-app
  namespace: gobeze-ai
  labels:
    app: gobeze-ai
    component: api
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: gobeze-ai
    component: api 